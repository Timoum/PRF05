jQuery(function ($) {
  const button = $(`<button> En Savoir PLus</button>`).appendTo(
    "body:last-child"
  );
  const section = section.hide;
  h1.hide();

  button.click(function () {
    h1.toggle();
  });
});
